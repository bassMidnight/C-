#include <iostream>
#include <cmath>
using namespace std;

int main(){

    int Y,Z;
    float X;
    cout << "Enter number (Y Z)";
    cin >> Y >> Z ;

    X = 5*Y + (((3*Z)-1)/(4*((3*Z)+1)-Y)) ;
    E = D/B*((A + 4)/(C + 1))
    E = 5 * A / D * (B + 1)

/*
    เงินเหลือ + ราคาสินค้า < วงเงินอนุมัติ
    ใช้จ่าย < 45Day
     != หมดอายุ
*/
}